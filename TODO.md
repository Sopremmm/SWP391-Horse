# TODO

- [ ] Fix HorseOwnerHome header UI not applying CSS (Header.tsx not using classes from Header.css)
- [ ] Add import Header.css to src/components/common/Header.tsx
- [ ] Wrap header markup with .header and .header-inner classes (or replace with existing ones) so existing CSS applies
- [ ] Run build/test to confirm no TypeScript/React errors

